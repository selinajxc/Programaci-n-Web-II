import { Routes } from '@angular/router';

/**
 * ==========================================================
 * RUTAS DE LA APLICACIÓN
 * ==========================================================
 *
 * Este archivo define las rutas de navegación de Angular.
 *
 * En este ejemplo todavía no estamos manejando navegación
 * entre varias pantallas, por eso el arreglo queda vacío.
 *
 * Aun así, el archivo se mantiene porque forma parte
 * de la estructura moderna de Angular y deja preparado
 * el proyecto para crecer después.
 *
 * Más adelante aquí podrías agregar rutas como:
 *
 * - /contactos
 * - /contactos/nuevo
 * - /contactos/editar/:id
 * - /contactos/detalle/:id
 */
export const routes: Routes = [];